// Checkbox.js
import React from "react";

const Checkbox = ({ label }) => {
  // Checkbox bileşeninin içeriği...
  return (
    <div>
      <Checkbox>{label}</Checkbox>
    </div>
  );
};

export default Checkbox; // Varsayılan (default) export
